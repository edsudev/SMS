@model EDSU_SYSTEM.Models.Payment

@{
    ViewData["Title"] = "Wallet";
}
<div class="main-container" id="container">
        <partial name="_StudentDashboardPartial" />
        <!--  BEGIN CONTENT AREA  -->
        <div id="content" class="main-content">
            <div class="layout-px-spacing">

                <div class="row layout-top-spacing">
                    <div class="col-6 mt-3 mx-auto">
                        <img src="~/images/wallet.svg" style="height:60vh; width:100%" />
                    </div>
                    <div class="col-4 mx-auto">
                        <div class="alert alert-arrow-right alert-icon-right alert-light-danger mb-4" role="alert">
                            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-alert-circle"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12" y2="16"></line></svg>
                            <strong>Note!</strong>
                            - Proceeding will deduct your credit and activate your acceptance fee.
                        </div>
                        <div class="col-md-12 widget-content widget-content-area p-3">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Description:</th>
                                        <td>@Model.OtherFees.Name</td>
                                    </tr>
                                    <tr>
                                        <th>Amount:</th>
                                        <td>@Model.Amount</td>
                                    </tr>
                                    <tr>
                                        <th>Order ID:</th>
                                        <td>@Model.Ref</td>
                                    </tr>
                                    <tr>
                                        <th>Desc:</th>
                                        <td>@Model.Status</td>
                                    </tr>
                                    <tr>
                                        <th>Session:</th>
                                        <td>@Model.Sessions.Name</td>
                                    </tr>
                                    <tr>
                                        <th>Payment Date:</th>
                                        <td>@Model.PaymentDate</td>
                                    </tr>

                                </thead>

                            </table>

                        <form asp-action="UpdateOtherPayment">
                                <input type="hidden" asp-for="Ref" />
                                <input type="submit" value="Proceed" class="btn btn-primary" />
                            </form>
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <!--  END CONTENT AREA  -->
    </div>